#include "color.h"

#define primaryColor (Color_FG_DEFAULT)
#define secondaryColor (Color_FG_BLUE)
#define resetColor (Color_FG_WHITE)

// toggleable fields
#define displayHostname (true)
#define displayKernelVersion (true)
#define displayOperatingSystem (true)
#define displayShell (true)
#define displayPackages (true)
#define displayCpu (true)
